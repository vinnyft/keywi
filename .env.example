# ============================================================
# Keywi — modèle de variables d'environnement
# Copier vers .env.local puis compléter.
# Les valeurs Supabase ci-dessous sont celles de la stack locale
# (identiques pour tout le monde, affichées par `supabase status`).
# ============================================================

# Supabase (local)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# URL publique du site (liens dans les emails)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe (mode test) — clés sk_test_/pk_test_ depuis dashboard.stripe.com
# Laisser vide en local : le paiement est alors « simulé », validé
# d'office. Ce repli n'existe QU'EN DÉVELOPPEMENT — en production,
# une clé absente fait refuser le dépôt au lieu de l'offrir.
STRIPE_SECRET_KEY=
# (Aucune clé publiable côté client : le paiement passe par Stripe
#  Checkout hébergé, atteint par simple redirection.)
# Obligatoire dès que STRIPE_SECRET_KEY est renseignée : sans lui, le
# webhook refuse tout (un événement non signé n'est pas vérifiable).
# En local, `stripe listen` affiche le whsec_… à recopier ici.
STRIPE_WEBHOOK_SECRET=

# Resend — clé re_... depuis resend.com
# Laisser vide pour journaliser les emails dans la console du serveur
RESEND_API_KEY=
EMAIL_FROM=Keywi <notifications@keywi.fr>

# Tâches planifiées — protège /api/cron/*
# Vide en local (les routes restent appelables à la main pour tester).
# EN PRODUCTION : mettre une valeur longue et aléatoire, sinon
# n'importe qui peut déclencher les relances et la génération de codes.
#   openssl rand -base64 32
CRON_SECRET=

# Limitation de débit — poivre du hachage des emails et des IP stockés
# dans la table `tentatives` (voir src/lib/limitation.ts). Sans valeur,
# la clé service role sert de repli : ça fonctionne, mais un poivre
# dédié évite de faire dépendre deux secrets l'un de l'autre.
#   openssl rand -base64 32
RATE_LIMIT_PEPPER=
